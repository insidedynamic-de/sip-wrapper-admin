# Linkify TalkHub Admin Console — Environment Variables
# Copy to .env and adjust values

# Port the admin console listens on (default: 3000)
ADMIN_PORT=3000

# Docker image (for docker-compose pull-based deploy)
DOCKER_IMAGE=ghcr.io/insidedynamic-de/sip-wrapper-admin:latest
